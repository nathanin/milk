wide_args = {
    'depth_of_model': 48,
    'growth_rate': 64,
    'num_of_blocks': 4,
    'num_layers_in_each_block': 12,
}

deep_args = {
    'depth_of_model': 64,
    'growth_rate': 24,
    'num_of_blocks': 4,
    'num_layers_in_each_block': 16,
}

mnist_args = {
    'depth_of_model': 16,
    'growth_rate': 24,
    'num_of_blocks': 2,
    'num_layers_in_each_block': 8,
    'pool_initial': True
}
